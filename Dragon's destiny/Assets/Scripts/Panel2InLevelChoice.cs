using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Panel2InLevelChoice : MonoBehaviour
{
 /*   public GameObject panel2;

    [SerializeField]
    private EndOfFirstLevel countForChoice;

    void Start()
    {
        if (countForChoice.countOfOpenLevel == 2 || countForChoice.CountOfOpenLevel == 2)
        {
            panel2.SetActive(true);
        }
    }
*/

}
